[project]
name = "fastapi-book"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.115.2",
    "pytest>=8.3.3",
]

[tool.pytest.ini_options]
testpaths = ["test"]
